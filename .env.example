# ---------------------------------------------------------------------------
# Testimonials — environment configuration
# Copy this file to `.env.local` locally, fill in the values, and set the same
# variables in Vercel (Project Settings → Environment Variables).
# Local and production share the SAME TESTIMONIALS_DSN so both talk to Neon.
# ---------------------------------------------------------------------------

# Neon PostgreSQL connection string (production + local).
# Use the pooler connection string (host looks like <project>.<region>.pooler.aws.neon.tech)
# for serverless. TLS is enabled automatically for *.neon.tech hosts by the
# PostgresStore, so a plain postgres:// URL works too.
#
# Example:
# TESTIMONIALS_DSN=postgresql://neondb_owner:password@ep-abc-123.us-east-2.aws.neon.tech/neondb?sslmode=require
#
# The standard DATABASE_URL (e.g. pulled by `neon link`) is also accepted.
# DATABASE_URL=postgresql://neondb_owner:password@ep-abc-123-pooler.aws.neon.tech/neondb?sslmode=require

# Secret token required to approve/reject/delete testimonials.
# Must match on local and production for moderate via curl.
TESTIMONIALS_ADMIN_TOKEN=change-me-to-a-long-random-string

# Optional webhook. On each submission a payload (with signed approve/reject
# links) is POSTed here; if unset, the payload is logged to the server console.
# TESTIMONIALS_NOTIFY_URL=https://example.com/hooks/testimonials

# ---------------------------------------------------------------------------
# Email notifications (Resend) — sends you the pending-testimonial + links
# ---------------------------------------------------------------------------

# Required to send email notifications. Create a key at resend.com/api-keys
# (API Keys → "Create API Key"). Keep it secret — it lives only on the server.
RESEND_API_KEY=re_xxxxxxxxxxxx

# The address that receives the new-testimonial notification email.
TESTIMONIALS_OWNER_EMAIL=you@example.com

# Optional. Sender shown on the email. Use an address on A DOMAIN YOU HAVE
# VERIFIED in Resend (Domains → Add Domain → verify DNS, then send from
# e.g. "Shubh.dev <noreply@yourdomain.com>"). If omitted, defaults to
# onboarding@resend.dev, which only works for sending to your own account
# until you verify a domain.
# TESTIMONIALS_EMAIL_FROM="Shubh's Portfolio <noreply@yourdomain.com>"

# ---------------------------------------------------------------------------
# Contact form — reuses the same RESEND_API_KEY as testimonials above.
# On each submission the site emails you the visitor's message (Name, Email,
# Company, Subject, Message + timestamp) with the visitor's email as Reply-To.
# ---------------------------------------------------------------------------

# The address that receives contact-form messages. If unset, falls back to
# TESTIMONIALS_OWNER_EMAIL so one Resend configuration covers both features.
CONTACT_OWNER_EMAIL=you@example.com

# Optional. Sender shown on contact emails. Falls back to TESTIMONIALS_EMAIL_FROM.
# CONTACT_EMAIL_FROM="Shubh's Portfolio <noreply@yourdomain.com>"

# ---------------------------------------------------------------------------
# Profile photo uploads (Neon Object Storage, S3-compatible).
# These are pulled automatically by `neon deploy` into `.env.local`, and you
# must add the same values in Vercel under the same names. They are injected
# together with the branch storage credentials; objects live in a public_read
# bucket ("testimonial-photos") so approved testimonials can load them.
# ---------------------------------------------------------------------------

# Endpoint of the branch's object storage (e.g. https://br-<branch>.storage.<region>.aws.neon.tech)
# AWS_ENDPOINT_URL_S3=https://br-xxxx.storage.ap-southeast-1.aws.neon.tech

# Region where the Neon project/branch lives (used for signing).
# AWS_REGION=ap-southeast-1

# Branch-scoped credentials for signing presigned PUT uploads.
# AWS_ACCESS_KEY_ID=nak_...
# AWS_SECRET_ACCESS_KEY=nsk_...